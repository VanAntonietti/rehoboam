use tachyonfx::{
    fx, CellFilter, Duration, Effect, EffectManager, EffectManager, EffectTimer, Interpolation,
};

pub fn startin_up_effect() -> Effect {
    let mut effects = vec![];
    let initial_delay = Duration::from_millis(300);
    let mut accrued_delay = initial_delay.as_millis();

    todo!()
    fx::parallel(&effects)
}
  c
